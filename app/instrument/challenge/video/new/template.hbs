{{#modal-dialog translucentOverlay=true close="closeModalDialog"}}
  <h3 {{action "closeModalDialog" on="click"}} class="exit">x</h3>
  <div class="section center">
    <h3 class="text__title section">Post Video</h3>
    {{#with (changeset formValues validator) as |changeset|}}
      <form {{action "submitVideo" changeset on="submit"}}>
        {{input type="url" value=changeset.videoUrl placeholder="URL" class="form"}}
        {{input type="text" value=changeset.title placeholder="Title" class="form"}}
        {{textarea type="text" value=changeset.description placeholder="Description" class="form"}}
        <div class="button-bar">
          {{#link-to 'instrument.challenge.video' class="button button--black button--fullwidth"}}
            Back
          {{/link-to}}
          <button class="button button--black button--fullwidth">Submit</button>
        </div>
      </form>
    {{/with}}
  </div>
{{/modal-dialog}}
